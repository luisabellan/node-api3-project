"use strict";var express=require("express"),cors=require("cors"),logger=require("./server"),postRouter=require("./posts/postRouter"),userRouter=require("./users/userRouter"),server=express(),port=4e3;server.use(express.json()),server.use(cors());var router=express.Router();server.use("/users",logger,userRouter),server.use("/posts",logger,postRouter),server.use(function(e,r){r.status(404).json({message:"Route was not found"})}),server.listen(port,function(){console.log("Server running at http://localhost:".concat(port))});