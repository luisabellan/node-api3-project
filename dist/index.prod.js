"use strict";var express=require("express"),cors=require("cors"),logger=require("./middleware/logger"),postRouter=require("./posts/postRouter"),userRouter=require("./users/userRouter"),server=express(),port=4e3;server.use(express.json()),server.use(cors()),server.use(logger({format:"long"}));var router=express.Router();server.use("/users",userRouter),server.use("/posts",postRouter),server.use(function(e,r){r.status(404).json({message:"Route was not found"})}),server.listen(port,function(){console.log("Server running at http://localhost:".concat(port))});